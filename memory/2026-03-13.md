# Memory: 2026-03-13

## CruiserConnect App - Route Generation Fixes

### UI Improvements (Commit a68d933)
- Fixed duplicate bottom sheet in cruise mode page
- Integrated route type selector (Direct/Sport/Adventure/Relaxed) into the A-to-B setup card
- Removed the separate bottom panel that was overlapping with main setup UI
- Now shows: Target location → Route type buttons → Calculate route button

### Navigation Language Fix (Commit 54aa739)
- Added `'language': 'de'` to API body in `generateRoundTrip()` and `generatePointToPoint()`
- Backend now returns navigation instructions in German
- Distance formatting already implemented: "300 m" / "6,4 km"

### Previous Fixes (Earlier commits)
- **Kreis-Bug**: Route start now snaps to exact GPS position via `_snapRouteToStartPosition()`
- **Background Navigation**: Navigation continues when leaving cruise mode page (only stops on explicit "Back" button)

### Repository Cleanup
- Removed OpenClaw dashboard files from CruiserConnect repo (.openclaw/, openclaw_dashboard/, config files)
- Added .gitignore to prevent future dashboard file commits

Branch: `Route_generation/implementation_OpenClaw`
All 4 tasks completed and pushed.
