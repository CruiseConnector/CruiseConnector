org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# MAPBOX CONFIGURATION
# SDK_REGISTRY_TOKEN wird aus ~/.gradle/gradle.properties gelesen (NICHT hier eintragen!)
# Setup: In ~/.gradle/gradle.properties diese Zeile einfügen:
#   SDK_REGISTRY_TOKEN=sk.dein_mapbox_secret_token_hier
